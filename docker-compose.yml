services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: riepilogo
      POSTGRES_USER: app
      POSTGRES_PASSWORD: devpassword
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
